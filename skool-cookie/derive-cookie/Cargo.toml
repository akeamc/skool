[package]
name = "derive-cookie"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
quote = "1.0.14"
syn = { version = "1.0.85", features = ["parsing"] }

[lib]
proc-macro = true
